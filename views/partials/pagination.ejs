<div class="pagination">
	<% if (page !==0) { %>
		<a class='btn' href=<%=`${url}${queryString}page=${page-1}` %> >Previous Page</a>
		<% } %>
			<a class='btn' href=<%=`${url}${queryString}page=${page+1}` %> >Next Page</a>
</div>
